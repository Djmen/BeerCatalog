# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BeerCatalog' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BeerCatalog
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Action',     '~> 3.8.0' 
  pod 'SDWebImage', '~> 4.0'
end
